[project]
name = "scrapy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sweta Nakadi", email = "" }
]
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.62.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[project.scripts]
scrapy = "scrapy:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
